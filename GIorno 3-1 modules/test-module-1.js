class Calculator { // as in javascript
    add (a, b) {
        return a + b;
    }

    multiply(a, b) {
        return a * b;
    }

    divide(a, b) {
        return a/b;
    }
}

module.exports = Calculator; // when we want to export a single element